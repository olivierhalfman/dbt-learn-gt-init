sources:
  - name: jaffle_shop
    database: playground  
    schema: dbt_ohalfman
    config:
      freshness:
        warn_after:
          count: 12
          period: hour
        error_after:
          count: 1
          period: day
      loaded_at_field: order_date
    tables:
      - name: jaffle_shop_orders
      - name: jaffle_shop_customers
        freshness: null